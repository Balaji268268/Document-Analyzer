# DocSummarizer - Requirements
#
# Floors are pinned to clear known CVEs / advisories on the lower bound.
# Upper caps avoid pulling in upstream major-version breaks until tested.

# Core LLM
#   GHSA-56xg-wfcc-g829 (critical RCE via Jinja2 SSTI) fixed in 0.2.72.
#   0.3.x has breaking Llama() / GGUF changes — held back until verified.
llama-cpp-python>=0.2.72,<0.3

# Document parsing
#   pypdf <6.10.2 carries ~24 DoS / RAM-exhaustion advisories vs hostile PDFs.
pypdf>=6.10.2,<7
python-docx>=1.2.0,<2
striprtf>=0.0.32
chardet>=5.2.0,<6  # 7.x is a ground-up rewrite, defer

# GUI
customtkinter>=5.2.2,<6

# Utilities
#   huggingface-hub 1.x has API churn; stay on 0.x latest until migration.
huggingface-hub>=0.30.0,<1
#   tqdm <4.66.3 carries CVE-2024-34062 (CLI eval). Not exploited here but bumped.
tqdm>=4.66.3,<5
psutil>=6.0.0,<7
